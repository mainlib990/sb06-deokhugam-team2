spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dukhugam
    username: team02
    password: team02
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  sql:
    init:
      mode: never

  cloud:
    aws:
      s3:
        bucket: ${AWS_S3_BUCKET:111}

  naver:
    client-id: ${NAVER_CLIENT_ID:111}
    client-secret: ${NAVER_CLIENT_SECRET:111}

deokhugam:
  ocr:
    api-key: ${OCR_API_KEY:111}
